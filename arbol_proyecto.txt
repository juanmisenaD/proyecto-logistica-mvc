proyecto-logistica-mvc/
├── config/
│   ├── constants.js            # Tarifas base, % de seguro y límites (500 kg)
│   └── users.js                # Usuarios y credenciales por rol (ADMIN, OPERADOR, MENSAJERO)
├── data/
│   ├── clientes.json           # Persistencia de base de datos de clientes
│   ├── guias.json              # Persistencia del historial de guías y envíos
│   ├── nation.json             # Catálogo geográfico de departamentos y ciudades
│   └── tickets/                # Comprobantes generados
├── public/                     # Archivos estáticos servidos públicamente por Express
│   ├── css/
│   │   ├── bootstrap.min.css   # Bootstrap v5.1.3 CSS
│   │   └── style.css           # Estilos personalizados
│   └── js/
│       ├── bootstrap.bundle.min.js # Bootstrap v5.1.3 JS (con Popper incorporado)
│       ├── crear.js            # Validación dinámicas del peso disponible (DOMContentLoaded) y cascada de ciudades
│       └── main.js             # Modal de ticket con async/await (const { guia } = ...) y reloj del dashboard
├── src/
│   ├── controllers/
│   │   ├── apiController.js    # Endpoints REST (/api/guias/:id, /api/clientes) para AJAX/fetch
│   │   ├── authController.js   # Gestión de inicio y cierre de sesión (Login/Logout)
│   │   └── guiaController.js   # Lógica del Dashboard, Balance, Filtro de Fechas y Métricas de Capacidad
│   ├── middlewares/
│   │   └── authMiddleware.js   # Control de acceso y protección de rutas según el rol (RBAC)
│   ├── models/
│   │   ├── clienteModel.js     # I/O y consultas sobre clientes.json
│   │   ├── guiaModel.js        # I/O sobre guias.json y actualización de estados
│   │   └── nationModel.js      # Lectura de la división política (nation.json)
│   ├── routes/
│   │   └── logisticaRoutes.js  # Definición de rutas web protegidas y públicas
│   ├── services/
│   │   └── cronService.js      # Tareas programadas en segundo plano (node-cron a "EN CAMINO")
│   ├── utils/
│   │   └── validators.js       # Validaciones (DNI <= 10 dígitos, emails, cupo 500 kg, duplicados)
│   └── views/
│       ├── index.ejs           # Dashboard principal (Tarjetas de Balance, Filtros, Modal Ticket/Clientes)
│       ├── crear.ejs           # Formulario para registro de envíos con aviso de Capacidad Disponible
│       └── login.ejs           # Pantalla de acceso por credenciales
├── app.js                      # Servidor principal Express (Sesiones, Middleware y Cron Jobs)
└── package.json                # Dependencias (express, ejs, express-session, node-cron, etc.)